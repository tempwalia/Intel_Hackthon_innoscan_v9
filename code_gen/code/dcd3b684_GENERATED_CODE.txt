--- FILE: student_grade_calculator_v2/src/main.py ---
from flask import Flask, request, jsonify
import numpy as np
import pandas as pd
from sklearn.feature_extraction.text import TfidfVectorizer
from sklearn.metrics.pairwise import cosine_similarity

app = Flask(__name__)

# Load pre-trained model and vectorizer
model = tfidf_vectorizer = pickle.load(open('pretrained_model.pkl', 'rb'))

@app.route('/classify', methods=['POST'])
def classify_ticket():
    # Extract ticket text from JSON payload
    ticket_data = request.json
    ticket_text = ticket_data['ticket_text']

    # Vectorize the ticket text using TF-IDF
    X_test = tfidf_vectorizer.transform([ticket_text])

    # Predict the category using cosine similarity
    predicted_category = model.predict(X_test)[0]
    
    return jsonify({
        'status': 'success',
        'tickets': [{
            'ticket_id': 1,
            'category': predicted_category,
           