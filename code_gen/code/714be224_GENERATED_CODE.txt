--- FILE: student_grade_calculator/src/app.py ---
from flask import Flask, render_template, redirect, url_for, flash, request
from forms import StudentGradeForm
import sqlite3

app = Flask(__name__)
app.config['SECRET_KEY'] = 'your_secret_key'

def connect_db():
    return sqlite3.connect('student_grades.db')

@app.route('/')
def index():
    conn = connect_db()
    cursor = conn.cursor()
    cursor.execute("SELECT * FROM students")
    rows = cursor.fetchall()
    conn.close()
    return render_template('index.html', rows=rows)

@app.route('/add_student', methods=['GET', 'POST'])
def add_student():
    form = StudentGradeForm(request.form
    if form.validate_on_submit():
        # Logic to insert student into database goes here
        flash(f'Successfully added {form.name.data}!')
        return redirect(url_for('index'))
    return render_template('add_student.html', form=form)

if __name__ == '__main__':
    app.run(debug=True)

--- FILE: student_grade_calculator/config/db_config.py ---
DATABASE_URI = 'sqlite:///students.db'

--- FILE: student_grade_calculator/templates/index.html ---
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Student Grades</title>
</head>
<body>
    <h1>Welcome to Student Grade Calculator</h1>
    <a href="{{ url_for('add_student') }}">Add New Student</a><br><br>
    {% for row in rows %}
        <p>{{ row[0] }} - {{ row[1] }}</p>
    {% endfor %}
</body>
</html>

--- FILE: student_grade_calculator/templates/add_student.html ---
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Add New Student</title>
</head>
<body>
    <h1>Add New Student</h1>
    <form method="post">
        <label for="name">Name:</label>
        <input type="text" id="name" name="name"><br><br>
        <button type="submit">Add</button>
    </form>
</body>
</html>

--- FILE: student_grade_calculator/forms/student_grade_form.py ---
from flask_wtf import Form
from wtforms import StringField, validators

class StudentGradeForm(Form):
    name